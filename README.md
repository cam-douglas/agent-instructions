# Agent Instructions

Portable coding-agent control plane. The distributable tree is [`.cursor/`](.cursor/README.md).

Copy `.cursor/` into a project root, then run:

```bash
node .cursor/skills/launch-pipeline/scripts/preflight.mjs
bash .cursor/scripts/bootstrap.sh
```

MIT — see [LICENSE](LICENSE).
